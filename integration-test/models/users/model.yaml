version: 2
fields:
  firstName:
    type: string
    required : true
  lastName:
    type: String
  age:
    type: Long
    required: true
  locale:
    type: String
    validation: |
      locale.length >= 0
